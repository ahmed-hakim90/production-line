/** Shared skeleton surface tokens (tenant theme + RTL-safe). */
export const skeletonBlockClass =
  'bg-[var(--color-border)]/60 motion-reduce:animate-none';

export const skeletonCardClass =
  'rounded-[var(--border-radius-lg)] border border-[var(--color-border)] bg-[var(--color-card)]';
